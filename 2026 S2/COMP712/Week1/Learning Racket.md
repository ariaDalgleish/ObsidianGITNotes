
Expression
Can be a function call. To call a function use open parenthesis before the function name, then expressions for the function arguments, and then a close parenthesis: 
Using # lang slideshow to draw pictures!
```(circle 10)```
result: O 
	Picture value, circle and circle size in pixels. 
```(rectangle 10 20)```
	Prints rectangle which takes two arguments instead of one